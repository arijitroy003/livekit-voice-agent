[project]
name = "livekit-voice-agent"
version = "0.1.0"
description = "Prototype LiveKit voice agent from the production voice-agents workshop"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "livekit-agents[mcp,silero,turn-detector]~=1.3",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.3",
]
